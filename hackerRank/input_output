/* 
scala stdIn for reading inputs from hackerRank
*/

import scala.io.StdIn.{readInt,readLine}
// read int 
val n = readInt()

/*
read in pairs of integers (2 per line separately by space)
returns a list of ((Int,Int))
*/
val inputs = (1 to n).map(i=>readLine().split(" ")).map{
	case Array(x,y)=>(x.toInt,y.toInt)
	}.toList


